%% -*- mode: erlang;erlang-indent-level: 4;indent-tabs-mode: nil -*-
%% ex: ts=4 sw=4 ft=erlang et

[
    {duration, 10}, %% seconds
    %% length of data array. min, max, step.
    %% Step: {mult, N} or {add, N} to multiply or add by N
    {length, {10, 11000, {mult, 2}}},
    %% number of requests per array. min, max, step.
    %% Step: {mult, N} or {add, N} to multiply or add by N
    {requests, {1, 32, {mult, 2}}},
    %% number of repeats for particular length and requests
    {one_round_repeats, 10},
    {probability_of_repeat, 0.2} %% of a particular value in the array
    %% output for sort and median: x - len, y - n_reqs, z - time
].
